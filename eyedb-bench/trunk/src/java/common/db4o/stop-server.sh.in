#!/bin/bash
DB4O_JAR=@DB4O_JAR@
JAVA=@JAVA@

set -e

stop_server() {
    if test -f server.pid; then
	kill -1 `cat server.pid`
	rm -f server.pid
    else
	echo 'No server PID file, cannot stop server'
    fi
}

stop_server
