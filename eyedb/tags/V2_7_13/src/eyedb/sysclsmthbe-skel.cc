
/*
 * EyeDB Version 2.7.12 Copyright (c) 1995-2006 SYSRA
 *
 * File 'sysclsmthbe-skel.cc'
 *
 * Package Name 'syscls'
 *
 * Generated by eyedbodl at Fri Feb  9 14:01:42 2007
 *
 */

#include <eyedb/eyedb.h>

#include "syscls.h"

using namespace eyedb;

// To implement and use user methods, perform the following operations
/*

#1. Copy the skeleton file
cp sysclsmthbe-skel.cc sysclsmthbe.cc

#2. Implement the user methods in sysclsmthbe.cc using a text editor

#3. Compile the shared library

#4. Copy the shared library to the eyedb loadable library directory
cp sysclsmthbe-2.7.12.so <eyedbinstalldir>/lib/eyedb

#5. Change the file access mode
chmod a+r <eyedbinstalldir>/lib/eyedb/sysclsmthbe-2.7.12.so

*/

static Bool __syscls_init = False;

#define _packageInit(DB) \
 \
  if (!__syscls_init) { \
    syscls::init(); \
    __syscls_init = True; \
  } \
 \
  if (!(DB)->getUserData("eyedb:syscls")) { \
     Status s = sysclsDatabase::checkSchema((DB)->getSchema()); \
     if (s) return s; \
     sysclsDatabase::setConsApp(DB); \
     (DB)->setUserData("eyedb:syscls", (void *)1); \
  }

